services:
  tutor:
    build: .
    env_file: .env
    volumes:
      - .:/app
      - /tmp/inktutor:/tmp/inktutor

  dashboard:
    build: .
    command: ["python", "-m", "uvicorn", "dashboard:app", "--host", "0.0.0.0", "--port", "8080"]
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /tmp/inktutor:/tmp/inktutor

  api:
    build: .
    command: ["python", "-m", "uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /tmp/inktutor:/tmp/inktutor
